{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["yarn install"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": []
    }
  },
  "deploy": {
    "startCommand": "yarn ponder:start"
  }
}
